# Neon Postgres — required for server-side time log persistence
DATABASE_URL="postgresql://user:password@host/db?sslmode=require"

# Gemini — optional, for AI insights in Reports (server-side only)
GEMINI_API_KEY="your_gemini_api_key"
