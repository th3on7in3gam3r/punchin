[dev]
  command = "npm run dev"
  targetPort = 3000     # ← This is the important line (your Vite port)
  port = 8888           # Netlify's proxy port (default)

# Optional: make functions easier to find
[functions]
  directory = "netlify/functions"